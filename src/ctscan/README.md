# CT Scan Service

Chest CT pulmonary nodule triage service.

Status:
- `v1` scope is chest CT only
- findings are pulmonary nodules only
- UI is research/demo only, not diagnostic

Run locally from `src/ctscan`:

```bash
python main.py
```
