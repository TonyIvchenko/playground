name: playground
dependencies:
  - python>=3.8
  - pip
  - pip:
      - -r requirements.txt
      - -r src/test/requirements.txt
      - -r src/hurricane/requirements.txt
      - -r src/wildfire/requirements.txt
