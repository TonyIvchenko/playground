name: playground
dependencies:
  - python=3.11
  - pip
  - pytest
  - pip:
      - -r requirements.txt
      - -r src/test/requirements.txt
      - -r src/disasters/requirements.txt
      - -r src/ctscan/requirements.txt
